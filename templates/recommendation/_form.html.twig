{# ------------ M O B I L E ---------- #}
{{ form_start(form) }}
<div class="mt-3 text-center grid-form gap-3">
    {{ form_row(form.title) }}
    {{ form_row(form.content) }}
    {{ form_row(form.city) }}
    <button class="btn mt-5 mx-auto ">{{ button_label|default('Ajouter') }}</button>
    </div>
{{ form_end(form) }}