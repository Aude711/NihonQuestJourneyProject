{% extends 'base.html.twig' %}

{% block body %}
	{# --------------------------mobile--------------------------------- #}
{% if error %}
	<div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
{% endif %}
<div class="d-flex d-md-none flex-column align-items-center vh-100">
	<img src="{{ asset('build/images/portail-torii.png') }}" alt="torii cerisier en fleur" class="login-icon mb-5"/>
		<form action="{{ path('login') }}" method="post">
			<div>
				<label class="textaccount" for="pseudo">Pseudo</label>
				<input class="form-control" type="text" id="pseudo" name="_username" value="{{ last_username }}"/>
			</div>
			<div>
				<label class="textaccount mt-2" for="password">Mot de passe</label>
				<input class="form-control" type="password" id="password" name="_password"/>
			</div>
			<input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
			{# <a class="link-underline link-underline-opacity-0 noaccount" href="{{ path('forgot_password_request') }}">Mot de passe oublié</a> #}
			<div class="d-flex justify-content-center">
				<button class="container btn button-login mt-4" type="submit">Se connecter</button>
			</div>
			<div>
			    {# <a class="noaccount link-offset-2 link-underline link-underline-opacity-75" href="{{ path('register') }}">Pas de compte? Inscrivez-vous ici</a> #}
			</div>
			{# {% if app.user %}
				<div class="d-flex justify-content-center">
					<a href="{{path('logout')}}" type="button" class="btn button-crud mt-4 mb-3" tabindex="-1" role="button" aria-disabled="true">Deconnexion</a>
				</div>
			{% endif %} #}
		</form>
</div>

	{# ---------------------------- desktop----------------------------- #}
	<div class="d-none main d-md-block">
		<div class="login-desktop row row-cols">
			<img src="{{ asset('build/images/cherry-blossoms.jpg') }}" alt="photo de rue en cerisier" class="object-fit-cover back-picture col-md-5 p-0"/>
			<div class="col-md-7 p-0 ">
				<div class="login-desktop-cont m-auto h-100 w-100">
					{% if error %}
						<div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
					{% endif %}
					<div class="d-flex h-75 flex-column align-items-center justify-content-md-evenly">
						<form class="w-50" action="{{ path('login') }}" method="post">
							<div>
								<label class="textaccount mt-3" for="pseudo">Nom d'utilisateur</label>
								<input class="form-control" type="text" id="pseudo" name="_username" value="{{ last_username }}"/>
							</div>
							<div>
								<label class="textaccount mt-3" for="password">Mot de passe</label>
								<input class="form-control" type="password" id="password" name="_password"/>
							</div>

							<input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">

							{# <a class="link-offset-2 link-offset-3-hover link-underline-dark link-underline-opacity-0 link-underline-opacity-75-hover noaccount" href="{{ path('app_forgot_password_request') }}">Mot de passe oublié</a> #}

							<div class="d-flex justify-content-center">
								<button class="btn button-login mt-3" type="submit">Se connecter</button>
							</div>
							<div class="d-flex justify-content-center">
								{# <a class="noaccount link-offset-2 link-offset-3-hover link-underline-dark link-underline-opacity-0 link-underline-opacity-75-hover" href="{{ path('app_register') }}">Pas de compte? Inscrivez-vous ici</a> #}
							</div>
						</form>
					</div>
				</div>
			</div>
		</div>
	</div>
{% endblock %}
